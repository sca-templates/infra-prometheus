---
description: Start the Prometheus stack (compose up + wait for health).
agent: build
---

# Up

Run `make up` from the repo root and confirm prometheus becomes healthy
(`curl <http://127.0.0.1:9090/-/healthy>`).
